---
output:
  html_document: default
  pdf_document: default
params: 
    set_title: "Rapport d'élevage"
    s: na
    j: na
    pop: na
    poids: na
---
# Production

Au bout du `r params$j` eme jour d’elevage avec une population de `r params$pop` individus de l’espèce `r params$s` on peut esperer produire `r params$poids` T de matière sèche.

